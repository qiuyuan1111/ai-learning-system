[pytest]
# 注意：两个 agent 的内部包都叫 `src`，不可在同一进程内混合收集。
# 请用 scripts/run-all-tests.* 在各服务目录下分别运行。
asyncio_mode = auto
python_files = test_*.py
python_functions = test_*
filterwarnings =
    ignore::DeprecationWarning
    ignore:.*deprecated.*:Warning
