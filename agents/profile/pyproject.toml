[project]
name = "agent-profile"
version = "0.1.0"
description = "画像智能体 - 用户画像构建与增量更新"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.backends._legacy:_Backend"
